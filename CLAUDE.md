# Astra repo notes

On session start, scan `TODO_CALENDAR.md` for past-due items.
That file holds technical debt with explicit revisit dates —
ignoring past-due entries is how dual paths become permanent.

Architectural map: `docs/timeout_hierarchy.md`,
`scripts/e2e_smoke.py`, `services/stream/main.py`.
